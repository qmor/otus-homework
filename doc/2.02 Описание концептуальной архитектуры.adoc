=== Описание концептуальной архитектуры

Для обеспечения задач масштабирования, отказоустойчивости и возможности применения разных технологий для составных частей системы выбрана микросервисная архитектура. На схеме представлены следующие компоненты

[cols="2^8"]
|===
|Компонент | Описание

|Пользователи | Пользователи - сотрудники предприятия использующие для доступа к сервису - личные мобильные устройства на IOS и Android, десктопные и веб версии мессенджера

|Load Balancer | Балансировщик нагрузки (Nginx/AWS ALB) - распределяет входящий трафик между репликами API Gateway, обеспечивает отказоустойчивость и масштабируемость


|Api-gateway | Входная точка для подключения клиентских мессенждеров, их авторизации. Используется HTTPS и websocket. Маршрутизирует запросы от пользователя между компонентами системы. Реализует rate limiting, circuit breaker, retry логику

|Service Discovery | Динамический поиск сервисов (Consul/Kubernetes DNS) - позволяет сервисам находить друг друга без жесткой привязки IP-адресов

|Сервис авторизации | аутентифицирует пользователя - на основании информации из сервиса пользователей, предоставляет авторизацию на различные действия например блокировку пользователей - глобальное удаление сообщений. Также сервис имплементирует one time password, чтобы не смешивать общий пароль пользователя на предприятии и пароль для мессенждера

|Сервис директ чатов | Реализует функциональность директ чатов пользователь - пользователь - хранит данные чатов в БД PostgreSQL. Использует CQRS для разделения операций чтения и записи

|Сервис пользователей | Хранит информацию о пользователях (postgreSQL), полученную от внешней интеграции с LDAP предприятия, так и информацию которую пользователь указал о себе, статус блокировки пользователя (например в стучае увольнения) аватары и т.д. 

|Сервис медиа| Хранит и отдает мультимедиа материалы (картинки, документы, видео) хранящиеся в s3 хранилище. метаинформация о медия (пользователь - владелец, ссылка на s3) хранятся в postgreSQL. Для снижения нагрузки на канал для раздачи медиа используется CDN.

|Сервис групповых чатов| Реализует функциональность каналов и групповых чатов - испольует postgreSQL. Поддерживает треды, упоминания, хэштеги

|Сервис уведомлений| Реализует функциональность доставки пользователям сообщений из групповых и директ чатов онлайн и после взода пользователя после отутствия - использует postgreSQL
Также используется для взаимодействия с mail сервером для пересылки OTP пользователю

|Брокер сообщений | Kafka для асинхронного взаимодейтствия между сервисами. Кластер из 3+ брокеров с репликацией топиков обеспечивает отказоустойчивость

|Сервис поиска| Поиск пользователем по истории директ и групповых чатов и каналов Использует Elasticsearch. Индексирует сообщения в реальном времени

|Сервис безопасности | Реализует управление доступом и шифрованием данных на уровне транспорта (TLS). Подробное описание механизмов безопасности см. в разделе <<3.01 Схема защиты и управления доступом>>

===

Синхронные взаимодействия между сервисами по REST/gRPC, асинхронные через Kafka

Подробное описание протоколов и форматов взаимодействия см. в разделе <<2.12 Форматы взаимодействия>>